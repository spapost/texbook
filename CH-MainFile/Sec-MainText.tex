% % ==== Параграф в главе ==================================================
% % \chapter{Основной файл}\label{CH:MainFile}
% % ========================================================================

% % ==== Параграф ==========================================================
% % \section{Основной текст}\label{Sec:MainText} 
% % ========================================================================
% % Ссылки внутри параграфа
% % 	Параграф  		--- Sec:MainText
% % 	Подпараграф		--- SSec:MainText-1
% % 	Уравнение		--- eq:MainText-1
% % 	Рисунок			--- Fig:MainText-1
% % 	Таблица			--- Tab:MainText-1
% % ========================================================================


\section{Основной текст}\label{Sec:MainText}


Для определения основного текста исходного файла введём важное понятие \textit{окружения}.

\bi{Окружением}\index{Окружение} будем называть <<сдвоенную>> команду системы \LaTeX, выполняющую определённые операции над текстом, заключённым в <<тело окружения>>. В общем случае эта команда может иметь следующий вид:
\begin{verbatim}
\begin{<имя окружения>}[<параметры>]
  <Тело окружения>
\end{<имя окружения>}
\end{verbatim}

Параметр \verb|<имя окружения>| является обязательным и представляет собой строку "--- название окружения. Дополнительные \texttt{<параметры>} зависят от конкретного окружения и будут оговорены отдельно.

Под \verb|<телом окружения>| будем понимать ту часть текста, к которой применяется данное окружение, т.~е. текст, заключённый между командами начала \verb|\begin{<имя окружения>}| и окончания \verb|\end{<имя окружения>}| окружения.

Окружения в системе \LaTeX\ играют очень важную роль, а их количество и разнообразие очень сильно упрощает работу над текстом и вёрстку документа. Следует отметить, что каждому началу окружения \verb|begin| всегда должен соответствовать свой \verb|end| с тем же самым названием, иначе транслятор выдаст сообщение об ошибке.

\index{Текст!основной}\bi{Основным текстом} будем называть весь текст, помещённый в окружение {document}.

Таким образом, можно максимально конкретизировать общую структуру документа:
\begin{verbatim}
%Начало исходного файла
% Начало преамбулы
\documentclass{<класс>}
%  Определения, настройки и подключаемые пакеты
% Конец преамбулы

%  Начало основного текста
   \begin{document}
%     Смысловая часть текста и команды форматирования
   \end{document}
%  Конец основного текста
%Конец исходного файла
\end{verbatim}

Преамбула заканчивается строкой \verb|\begin{document}|, т.~е.
началом основного текста. Основной файл всегда заканчивается строкой
\verb|\end{document}|. Любой текст, идущий после неё, не
воспринимается \TeX'ом и выводиться не будет.


\endinput